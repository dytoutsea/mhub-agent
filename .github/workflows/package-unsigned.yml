name: Package Unsigned Desktop

on:
  workflow_dispatch:
  push:
    branches:
      - dev

permissions:
  contents: read

jobs:
  package:
    name: ${{ matrix.os }} unsigned artifact
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    steps:
      - name: Configure LF checkout on Windows
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.autocrlf false
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run ensure:platform-deps
      - name: Verify shared and desktop source
        shell: bash
        run: |
          npm run check
          npm run typecheck
          npm run test
          npm run mobile:native:check
          npm run expo:check
      - name: Export Expo Web renderer
        run: npm run build:mobile
      - name: Build unsigned installer
        run: npm run package:unsigned --workspace @mhub/desktop
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - name: Upload unsigned artifact
        uses: actions/upload-artifact@v4
        with:
          name: mhub-agent-${{ matrix.os }}-unsigned
          path: apps/desktop/release/*
          if-no-files-found: error
          retention-days: 14
