name: Package Unsigned Desktop

on:
  workflow_dispatch:
  push:
    branches:
      - dev

permissions:
  contents: read

jobs:
  package:
    name: ${{ matrix.os }} unsigned artifact
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    steps:
      - name: Configure LF checkout on Windows
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.autocrlf false
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run ensure:platform-deps
      - name: Verify shared and desktop source
        shell: bash
        run: |
          npm run check
          npm run typecheck
          npm run test
      - name: Validate mobile native boundary on macOS
        if: runner.os == 'macOS'
        shell: bash
        run: |
          npm run mobile:native:check
          npm run expo:check
      - name: Export Expo Web renderer
        run: npm run build:mobile
      - name: Build unsigned Windows installer
        if: runner.os == 'Windows'
        run: npm run package:unsigned --workspace @mhub/desktop
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - name: Build unsigned macOS Universal installer
        if: runner.os == 'macOS'
        run: npm run package:unsigned:mac:universal --workspace @mhub/desktop
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - name: Upload unsigned Windows executable
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: mhub-agent-windows-latest-unsigned
          path: apps/desktop/release/*.exe
          if-no-files-found: error
          retention-days: 14
      - name: Upload unsigned macOS disk image
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: mhub-agent-macos-latest-unsigned
          path: apps/desktop/release/*.dmg
          if-no-files-found: error
          retention-days: 14
